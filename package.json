{
	"name": "generic-conference-landing",
	"type": "module",
	"version": "0.0.1",
	"scripts": {
		"dev": "astro dev",
		"build": "astro build",
		"preview": "astro build && wrangler pages dev",
		"astro": "astro",
		"deploy": "astro build && wrangler pages deploy",
		"cf-typegen": "wrangler types"
	},
	"dependencies": {
		"@astrojs/cloudflare": "^12.2.4",
		"astro": "^5.5.3"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20250319.0",
		"wrangler": "^4.2.0"
	}
}